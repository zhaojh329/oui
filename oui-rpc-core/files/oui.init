#!/bin/sh /etc/rc.common

START=50

USE_PROCD=1

start_service() {
	procd_open_instance
	procd_set_param command eco /usr/sbin/oui
	procd_set_param respawn
	procd_close_instance
}

service_triggers() {
	procd_add_reload_trigger "oui"
}
